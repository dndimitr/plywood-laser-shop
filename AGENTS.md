# Agent notes — ЛазерШперплат

Този проект следва плана в `.cursor/rules/project-plan.mdc`.

Приоритет: Neon Postgres, български UI, guest checkout, админ с Auth.js, Vercel Blob (с локален fallback).
